stdin: true
name: trading-news-agent
summary: An AI agent that tracks a user-defined market or sector, identifies key companies, fetches recent news, updates relevant facts, and generates hourly investment recommendations based on the gathered information.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini AI service.