stdin: true
name: Trading News Agent
summary: This agent identifies companies based on a user-provided market/sector/theme, fetches recent news for them, updates relevant facts, and generates investment recommendations. It runs in an hourly cycle, continuously monitoring and providing insights.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini AI service.