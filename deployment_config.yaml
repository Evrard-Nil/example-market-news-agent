stdin: true
name: trading-news-agent
summary: An AI agent that tracks financial news, updates facts, and generates investment recommendations for companies within a user-specified market, sector, or theme. It operates in continuous hourly cycles.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini API.