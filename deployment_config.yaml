stdin: true
name: trading-news-agent
summary: An AI agent that tracks financial markets, fetches news for specified companies, updates relevant facts, and generates investment recommendations based on the gathered information and sentiment analysis.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini API.