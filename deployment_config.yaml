stdin: true
name: trading-news-agent
summary: An AI agent that tracks financial markets, fetches news for specific companies, analyzes sentiment, and generates hourly investment recommendations based on the gathered information.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required to authenticate with the Google Gemini API, used for AI tasks like content generation, news aggregation, and recommendation generation.