stdin: true
name: trading-news-agent
summary: An AI agent that identifies companies based on a user-provided market/sector/theme, fetches recent news for these companies, processes the news into facts, and generates hourly investment recommendations.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key for authenticating with the Google Gemini AI service, essential for making API calls to the Gemini model.