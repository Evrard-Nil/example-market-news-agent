stdin: true
name: Trading News Agent
summary: An AI agent that identifies companies based on a user-provided market/theme, fetches relevant news, updates financial facts, and generates hourly investment recommendations for the tracked companies.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini API.