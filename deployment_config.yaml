stdin: true
name: trading-news-agent
summary: An AI agent that identifies companies based on a market/sector query, fetches news for them, updates relevant facts, and generates hourly investment recommendations.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key for authenticating with the Google Gemini API.