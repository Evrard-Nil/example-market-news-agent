stdin: true
name: trading-news-agent
summary: An AI agent that tracks companies based on a user-defined market or sector, fetches relevant news, analyzes sentiment, and generates investment recommendations. It operates in hourly cycles, continuously updating facts and recommendations.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini API.