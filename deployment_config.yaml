stdin: true
name: trading-news-agent
summary: An AI agent that tracks financial news for a specified market/sector/theme, identifies relevant companies, fetches news, updates facts, and generates investment recommendations hourly.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key for authenticating with the Google Gemini API, required for making calls to the generative AI models.