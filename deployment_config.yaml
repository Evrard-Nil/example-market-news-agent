stdin: true
name: Trading News Agent
summary: An AI agent that tracks financial news for user-specified markets, sectors, or themes. It identifies relevant companies, fetches recent news, updates facts, and generates investment recommendations based on the gathered information.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required to authenticate with the Google Gemini AI service.