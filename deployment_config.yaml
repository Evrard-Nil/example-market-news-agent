stdin: true
name: trading-news-agent
summary: An agent that tracks financial markets, fetches news for specified companies, analyzes sentiment, and generates investment recommendations in an hourly cycle.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key for authenticating with the Google Gemini API.