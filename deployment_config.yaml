stdin: true
name: Trading News Agent
summary: An AI agent that tracks financial news for user-specified markets, sectors, or themes. It identifies key publicly traded companies, fetches recent news, updates facts, and generates hourly investment recommendations based on sentiment and events.
environment_variables:
  - name: GEMINI_API_KEY
    value: <your_gemini_api_key>
    description: The API key required for authenticating with the Google Gemini API.